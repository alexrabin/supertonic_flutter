name: supertonic_flutter
description: High-quality multilingual text-to-speech engine for Flutter. Powered by Supertonic 3 with 31 languages, expression tags, and multiple voice styles.
version: 2.0.0
homepage: https://github.com/supertone-inc/supertonic
repository: https://github.com/YofarDev/supertonic_flutter
issue_tracker: https://github.com/YofarDev/supertonic_flutter/issues

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  plugin_platform_interface: ^2.1.0
  flutter_onnxruntime: ^1.6.0
  audioplayers: ^6.7.1
  logger: ^2.0.2
  dio: ^5.9.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# The following section is specific to Flutter plugins.
flutter:
  plugin:
    platforms:
      android:
        package: fr.yofardev.supertonic_flutter
        pluginClass: SupertonicFlutterPlugin
      ios:
        pluginClass: SupertonicFlutterPlugin
      macos:
        pluginClass: SupertonicFlutterPlugin
      linux:
        pluginClass: SupertonicFlutterPlugin
      web:
        pluginClass: SupertonicFlutterWebPlugin
        fileName: supertonic_flutter_web.dart
